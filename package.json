{
  "name": "graphql-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "prisma:migrate": "prisma migrate dev --name init_schema",
    "prisma:generate": "prisma generate",
    "prisma:reset": "prisma migrate reset --force",
    "prisma:push": "prisma db push",
    "prisma:seed": "node prisma/seed.js",
    "dev": "npm run docker:up && npm run prisma:push && npm run prisma:seed && npm run start",
    "dev:migrate": "npm run docker:up && npm run prisma:migrate && npm run prisma:seed && npm run start",
    "dev:reset": "npm run docker:up && npm run prisma:reset && npm run prisma:push && npm run prisma:seed && npm run start"
  },
  "author": "",
  "license": "ISC",
  "description": "Apollo Serverを使用したシンプルなGraphQLサーバーのテンプレート",
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "apollo-server": "^2.19.0",
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.5.0",
    "graphql": "^15.4.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "prisma": "^6.7.0"
  }
}
